pipeline {
  agent any
  stages {
    stage('pre-build') {
      steps {
        echo 'pre-commit stage'
      }
    }

    stage('build') {
      steps {
        echo 'build'
      }
    }

    stage('deploy') {
      steps {
        echo 'deploy'
      }
    }

  }
}